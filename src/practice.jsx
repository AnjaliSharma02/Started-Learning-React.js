export const Practices = () =>{
    const students = [];
    return (
        <>
        <p>{students.length && "no student found"}</p>
        <p>Number of students: {students.length}</p>
        </>
    )

}